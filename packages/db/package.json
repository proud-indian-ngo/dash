{
  "name": "@pi-dash/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "check:types": "tsc --noEmit --checkers 2",
    "db:down": "docker compose down",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun --env-file=../../.env --env-file=../../.env.worktree run scripts/migrate.ts",
    "db:push": "drizzle-kit push",
    "db:start": "bun --env-file=../../.env --env-file=../../.env.worktree run docker compose up -d",
    "db:stop": "docker compose stop",
    "db:studio": "drizzle-kit studio",
    "db:watch": "bun --env-file=../../.env --env-file=../../.env.worktree run docker compose up",
    "test:unit": "bun --bun vitest run",
    "whatsapp:start": "bun --env-file=../../.env --env-file=../../.env.worktree run docker compose up -d whatsapp",
    "whatsapp:stop": "docker compose stop whatsapp"
  },
  "dependencies": {
    "@pi-dash/env": "workspace:*",
    "@pi-dash/shared": "workspace:*",
    "drizzle-orm": "catalog:",
    "uuidv7": "catalog:"
  },
  "devDependencies": {
    "@pi-dash/config": "workspace:*",
    "@types/bun": "catalog:",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:"
  }
}
